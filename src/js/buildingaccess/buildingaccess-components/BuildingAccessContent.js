import React from 'react';

import BuildingAccessCards from './BuildingAccessCards';

export default class BuildingAccessContent extends React.Component {
  render() {
    return (
      <div>
        <BuildingAccessCards />
      </div>
    );
  }
}
